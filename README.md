# PlanIt Green (MVP)


Open-source scoring for supermarket oat milks in the UK. Transparent dataset + simple website.


## Packages
- `packages/planit-core` – scoring library
- `packages/planit-config` – tunable weights
- `apps/web` – Next.js app with API route handlers
- `data/` – CSV datasets